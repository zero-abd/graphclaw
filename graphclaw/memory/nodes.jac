# graphclaw/memory/nodes.jac
# Minimal shared memory enums used by the current workspace-backed memory layer.

enum MemoryType {
    USER = "User",
    FEEDBACK = "Feedback",
    PROJECT = "Project",
    REFERENCE = "Reference"
}
